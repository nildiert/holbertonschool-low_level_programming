#include "holberton.h"

/**
 * mul - Function that print the multiplication of two numbers
 * @a: First number that receives the function
 * @b: Second numberthat receives the function
 * Return: On success 1.
 */
int mul(int a, int b)
{
	int res = a * b;

	return (res);
}
