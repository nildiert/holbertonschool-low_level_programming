#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  printf("The computer programing it’s like building a multilingual puzzle with proper grammar, but the outcome is a piece of art, and that piece of art is useful\n");
  return (0);
}
