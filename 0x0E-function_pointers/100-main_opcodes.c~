#include <stdio.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(int args, char argv[])
{
	if (args != 2)
	{
		printf("Error\n");
		exit(1);
	}
	if (args < 0)
	{
		printf("Error\n");
                exit(1);
	}

	return (0);
}
